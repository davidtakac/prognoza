<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@android:id/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/app_widget_background"
    android:gravity="center_vertical"
    android:orientation="vertical"
    android:padding="16dp"
    android:theme="@style/Theme.Prognoza.Widget"
    tools:layout_height="180dp"
    tools:layout_width="180dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/iv_weather_icon"
            android:layout_width="56dp"
            android:layout_height="56dp"
            tools:src="@drawable/rainshowers_day" />

        <TextView
            android:id="@+id/tv_temperature"
            android:layout_width="wrap_content"
            android:layout_height="56dp"
            android:layout_gravity="center_horizontal"
            android:gravity="center"
            android:maxLines="1"
            android:textColor="?attr/widgetTextPrimaryColor"
            android:textSize="42sp"
            tools:text="180°" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_place"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="?attr/widgetTextSecondaryColor"
        android:textSize="16sp"
        tools:text="Osijek" />

    <TextView
        android:id="@+id/tv_feels_like"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="?attr/widgetTextSecondaryColor"
        android:textSize="16sp"
        tools:text="Feels like 225°" />

    <TextView
        android:id="@+id/tv_precipitation_forecast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="2"
        android:textColor="?attr/widgetTextSecondaryColor"
        android:textSize="16sp"
        tools:text="No precipitation next two hours" />

</LinearLayout>