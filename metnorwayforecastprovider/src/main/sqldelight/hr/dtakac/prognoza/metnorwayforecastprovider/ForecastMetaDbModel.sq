CREATE TABLE ForecastMetaDbModel (
    `latitude` REAL NOT NULL,
    `longitude` REAL NOT NULL,
    `expires` TEXT,
    `lastModified` TEXT,
    PRIMARY KEY(`latitude`, `longitude`)
);

get:
SELECT * FROM ForecastMetaDbModel
WHERE abs(latitude - :latitude) < 0.00001 AND abs(longitude - :longitude) < 0.00001;

insert:
REPLACE INTO ForecastMetaDbModel (latitude, longitude, expires, lastModified)
VALUES (?, ?, ?, ?);